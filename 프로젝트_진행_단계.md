# 프로젝트 진행 단계
---
## 전체 프로세스
1. 데이터 수집 및 프로젝트 기획 — 완료
2. 데이터 전처리 — 완료
3. 데이터 분석 및 EDA — 완료
4. 머신러닝 회귀 모델 구축 및 평가 — 완료
5. 모델 성능 향상 — 완료
6. 최종 결과 정리 및 대시보드 작성 — 완료

## 노트북 ↔ 단계 대응

| 노트북 | 담당 STEP | 상태 |
|---|---|---|
| `01_eda.ipynb` | 프로젝트 정의 · 데이터 정의서 | 완료 |
| `02_preprocessing.ipynb` | STEP 0~4 (정제 · 파생변수 28개) | 완료 |
| `03_split_and_eda.ipynb` | STEP 5 (분할) + EDA 1~7 | 완료 |
| `04_feature_selection_and_encoding.ipynb` | STEP 6~7 (변수 선택 · 인코딩 · 스케일링) | 완료 |
| `05_modeling.ipynb` | 회귀 모델 4종 구축 및 평가 | 완료 |
| `06_model_improvement.ipynb` | 성능 향상 6가지 시도 | 완료 |
| `app.py` + `feature_extraction.py` | Streamlit 대시보드 (5개 탭) | 완료 |

## 최종 성능 요약

| 단계 | 모델 | MAE | MSE | RMSE | R² |
|---|---|---|---|---|---|
| 베이스라인 | 평균 예측 | 0.4700 | 0.3358 | 0.5795 | 0.0000 |
| 베이스라인 | 길이 단독 | 0.4395 | 0.3118 | 0.5583 | 0.0716 |
| STEP 4 | Lasso 단일 | 0.3637 | 0.2185 | 0.4674 | 0.3421 |
| **STEP 5** | **앙상블 (최종)** | **0.3605** | **0.2154** | **0.4641** | **0.3515** |

----------
## 세부 프로세스

#### 1. 데이터 수집 및 프로젝트 기획
##### 주요 내용
[v] 자유 주제 선정 - ELL(L2)
[v] 예측 대상(Target) 정의 - Vocabulary 지표
[v] 데이터 수집 - https://github.com/scrosseye/ELLIPSE-Corpus/tree/main
[v] 데이터 구조 및 변수 확인 - text_id_kaggle, full_text 등 총 26개 컬럼
[v] 분석 목적 및 가설 설정 - "TTR은 길이에 교란되어 있다." 등 7개의 가설 설계
    → 검증 결과: 3개 확인 / 2개 기각 / 1개 부분 / 1개 재해석 (02 정의서)

#### 2. 데이터 전처리
[v] 결측치 확인 및 처리 - 논문과 관련 연구 기준에 어휘 점수만을 위한 개별 기준을 적용하여 처리
[v] 이상치 확인 및 처리 - 논문과 관련 연구 기준에 어휘 점수만을 위한 개별 기준을 적용하여 처리
[v] 중복 데이터 제거 - 근사 중복 탐지 방식을 통해 데이터 2건 제거
[v] 데이터 타입 변환 - 변환할 데이터 x
[v] 범주형 데이터 처리 - prompt 44종 → train 기준 30편 미만 OTHER 통합 → 37종 (04 STEP 7-1)
[v] Feature Selection - 규칙 R0~R5 적용, 파생 28개 → 15개 (04 STEP 6)
[v] Encoding, Scaling - 원핫 36개 + log1p 3개 + StandardScaler, 전부 train에만 fit (04 STEP 7)
[v] 학습/테스트 데이터 분리 - Vocabulary 층화 8:2, train 2997 / test 750 (03 STEP 5)

#### 3. 데이터 분석 및 EDA  ※ 03 노트북에서 완료
[v] 데이터의 기초 통계 분석 - EDA 1
[v] Target 변수 분포 확인 - EDA 2
[v] Feature별 분포 확인 - EDA 3 (왜도·첨도)
[v] Feature와 Target의 관계 분석 - EDA 4 (피어슨·스피어만·길이 교란)
[v] 상관관계 분석 - EDA 5 (히트맵 + VIF)
[v] 시각화를 통한 데이터 특성 파악 - EDA 2~7 전반
[v] (추가) 공정성 점검 - EDA 6 / prompt 편중 점검 - EDA 7

##### 분석 시 확인할 사항
[v] 어떤 Feature가 Target과 관계가 높은가? - guiraud(.380), MTLD_valid(.346), oov_ratio(-.315)
[v] Feature간 상관관계가 높은 변수는 없는가? - 0.85 초과 8쌍, VIF 10 이상 11개 → STEP 6에서 정리
[v] 이상치가 존재하는가? - 길이 IQR 이탈 86건, MTLD 무효 18건 (제거 대신 플래그 보존)
[v] 데이터 분포가 한쪽으로 치우쳐 있는가? - Target 왜도 +0.204, 상위 3구간이 78.9%
[v] 회귀모델 학습에 적합하지 않은 Feature는 없는가? - |r|<0.05 9개, 완전 종속 5개 → STEP 6에서 제거

#### 4. 머신러닝 회귀 모델 구축 및 평가  ※ 05 노트북에서 완료
[v] 회귀 알고리즘을 활용하여 예측 모델을 구축한다.

##### 회귀 모델 (4종 비교 — 배점상 2개 이상 시 A)
[v] Linear Regression - CV R² 0.3119
[v] Ridge - CV R² 0.3121 (alpha 30종 탐색)
[v] Lasso - CV R² 0.3125 (alpha 30종 탐색) → STEP 4 최종 채택
[v] Random Forest Regressor - CV R² 0.3053

##### 평가 지표 (STEP 4 최종 모델 기준)
[v] MAE - 0.3637
[v] MSE - 0.2185
[v] RMSE - 0.4674
[v] R2 - 0.3421

##### 필수 비교
[v] 선형 계열 vs 트리 계열 - 선형이 근소 우세, 단 폴드 간 표준편차(±0.044) 이내
[v] prompt 더미 포함 vs 미포함 - 4개 모델 전부에서 증가폭이 노이즈 범위 → 미포함 채택

##### 추가 수행
[v] 베이스라인 3종 (평균 / 길이 단독 / 핵심 4개)
[v] 잔차 분석 - 03 EDA 2의 예고(저점수 과대·고점수 과소)가 그대로 재현됨
[v] 계수 해석 + 부호 반전 점검 - herdan_c·k2_ratio·flag_very_short 3건 발견 → 해석 제외
[v] 예측 범위 절단 [1, 5] 및 0.5단위 반올림 검토 (반올림은 RMSE 악화로 기각)

#### 5. 모델 성능 향상  ※ 06 노트북에서 완료
[v] 기존 모델의 문제점을 분석하고 예측 성능을 개선한다.

##### 성능 향상 방법 (5개 항목 전부 시도, 채택·기각 근거를 수치로 기록)
[v] Feature 추가/제거 - 6개 조합 실험 → **채택** (부호 반전 3개 제거, 12개 변수)
[v] 이상치 재처리 - 윈저화 3개 분위 → 기각 (|Δ| ≤ 0.002, log1p가 이미 처리함)
[v] 데이터 전처리 방법 변경 - 희소 구간 표본 가중치 4단계 → 기각 (거래 관계 규명)
[v] 모델 변경 - GradientBoosting **채택** / HistGradientBoosting 기각 (표본 대비 과유연)
[v] Hyperparameter 조정 - RF 12조합 격자 → **채택** (0.3031 → 0.3155)
[v] (추가) 앙상블 - Lasso + RF + GB 균등 3분할 → **최종 모델** (5/5 폴드 개선)

##### 규명된 한계 3가지
- 관계가 거의 선형 → 비선형 모델(RF·GB·HistGB)이 선형 모델을 못 넘음
- 분포 불균형 → 끝단 편향은 앙상블로 해결 안 됨 (가중치는 전체 성능과 맞바꾸는 거래)
- 채점 자체의 불확실성 → Vocabulary의 채점자 간 일치도 Cohen's κ = .518 (7개 항목 중 최하위)

#### 6. 최종 결과 정리 및 대시보드 작성  ※ app.py에서 완료
[v] streamlit을 사용하여 결과 정리 및 예측 시스템을 구현한다.
[v] 프로젝트 소개 - 주제, 파이프라인 6단계, 설계 가설 7개의 검증 결과
[v] 데이터 정의 - 파생변수 7개 그룹, 선택된 15개 정의, 제거 규칙 R0~R5 + 전체 로그
[v] 데이터 분석 - 타겟 분포, 길이 교란 산점도, 상관 히트맵, 단변량 예측력
[v] 모델 성능 - 베이스라인→최종 진행표, 모델 4종 비교, 계수·중요도, 잔차, 개선 로그
[v] 예측 시스템 - 에세이 입력 → 점수 + 변수별 기여도 분해 + 신뢰 범위 고지

##### 실행
```bash
pip install -r requirements.txt
streamlit run app.py
```

##### 추가 산출물
- `feature_extraction.py` - 원문에서 변수 15개를 학습 때와 동일한 규칙으로 계산
- `validate_extraction.py` - 재계산값이 학습값과 일치하는지 test 750편으로 검증
- `README_dashboard.md` - 실행 가이드 + 발표 시 짚을 지점 + 예상 질문 대응

##### 재계산 검증 결과 (data/extraction_validation.json)
- 15개 중 14개 상관 0.99 이상 일치
- `MTLD_valid`만 상관 0.90 (ELLIPSE의 MTLD 계산 규칙 비공개 → 표준 알고리즘으로 재구현)
- 최종 예측 영향: 평균 0.019점 / 재계산 피처 test MAE 0.3598, R² 0.3529

##### 쓸 파일
```python
prep  = joblib.load('data/preprocessor.joblib')  # scaler · encoder · 프롬프트 그룹 통계
model = joblib.load('data/final_model.joblib')   # lasso + rf + gb 앙상블
```
- 모델 성능 탭: `final_test_results.csv`, `improvement_log.csv`, `feature_importance.csv`,
  `cv_results.csv`, `final_predictions.csv`
- 변수 목록: `selected_features.json` / 제거 근거: `feature_selection_log.csv`

##### 예측 시스템 구현 시 주의
1. 사용자 입력 원문에서 15개 변수를 **학습 때와 똑같이** 계산해야 함
   → 02 노트북의 파생변수 계산 코드를 함수로 떼어내 재사용
2. `len_z_in_prompt`는 프롬프트 그룹 통계 필요
   → `preprocessor.joblib`의 `token_prompt_stats` 사용, 처음 보는 주제는 `token_global`로 대체
3. 변환은 반드시 저장된 scaler/encoder 객체로 (코드를 다시 짜면 미세한 차이 발생)
4. 예측값은 `clip(1.0, 5.0)` 후 **화면 표시만** 0.5 단위 반올림
   (평가는 연속값 / 표시는 채점 형식 — 05 노트북 [4-6b] 참조)
5. 앙상블은 선형용(스케일링) 입력과 트리용(원값) 입력이 **다릅니다**
   → `final_model.joblib`의 `lasso_features` / `tree_features`를 각각 확인할 것
